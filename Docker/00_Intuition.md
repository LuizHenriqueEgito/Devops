# O que é o Docker

# Imagem

# Container

# Dockerfile

# Build

# Diferença entre **Container** e **Máquina Virtual**

# Docker Hub

---
# Comandos
`docker run`:
`docker pull`:
`docker ps`:
`docker stop` / `docker rm`:
